
sitefile: 0.0.7-dev
routes: {}

options:
  global:
    du:
      link_stylesheets: true
      scripts:
        $ref: '#/defs/scripts/default'

defs:
  scripts:
    default:
        - /vendor/jquery.js

# Id: node-sitefile/0.0.7-dev example/site/4/Sitefile.yml
