define([ 'jquery', 'lodash' ], function( $, _ ) {

  // Adapt Du/rST-XHTML to Twitter-Bootstrap CSS
  $('.document, .header, .footer').addClass('container');

  console.log('sitefile/client/base.js', $, _);
})
