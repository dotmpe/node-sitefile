#!/bin/bash

[ -n "$ENV" ] || ENV=development

#[ "$ENV" == "testing" ] && {
#}

version=0.0.3-test # node-sitefile

echo '{ "sitefilerc": "'$version'" }' > .sitefilerc

(
cat <<HEREDOC
module.exports =
  development: {}
HEREDOC
) > config/config.coffee

